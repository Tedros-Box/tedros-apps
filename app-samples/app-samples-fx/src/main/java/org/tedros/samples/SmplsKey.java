/**
 * 
 */
package org.tedros.samples;

/**
 * The language  resource key domain
 * 
 * Run the org.tedros.samples.fx.util.LanguageKeysBuilder to create the constants from your language resource.
 * Type the resource name without the locale, for example to read the App_en.properties just  
 * type App
 * 
 * @author Davis
 *
 */
public interface SmplsKey {

	static final String APP_SAMPLES = "#{app.samples}";
	static final String FORM_ORDER_STATUS = "#{form.order.status}";
	static final String FORM_PRICE = "#{form.price}";
	static final String FORM_SALES_STATUS = "#{form.sales.status}";
	static final String FORM_SALES_TYPE = "#{form.sales.type}";
	static final String UNIT_PRICE = "#{label.unit.price}";
	static final String MENU_CRUD_VIEWS = "#{menu.crud.views}";
	static final String MODULE_DESC_ORDERS = "#{module.desc.orders}";
	static final String MODULE_DESC_PRICE = "#{module.desc.price}";
	static final String MODULE_DESC_SALES = "#{module.desc.sales}";
	static final String MODULE_ORDERS = "#{module.orders}";
	static final String MODULE_PRICE = "#{module.price}";
	static final String MODULE_SALES = "#{module.sales}";
	static final String MSG_SELECT_COST_CENTER = "#{msg.select.cost.center}";
	static final String VIEW_ORDER_STATUS = "#{view.order.status}";
	static final String VIEW_ORDERS = "#{view.orders}";
	static final String VIEW_PRICE = "#{view.price}";
	static final String VIEW_SALES = "#{view.sales}";
	static final String VIEW_SALES_STATUS = "#{view.sales.status}";
	static final String VIEW_SALES_TYPE = "#{view.sales.type}";
}
